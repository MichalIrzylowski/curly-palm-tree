import type { Block } from 'payload'

export const MapBlock: Block = {
  slug: 'mapBlock',
  interfaceName: 'MapBlock',
  labels: {
    singular: 'Map & Directions',
    plural: 'Map & Directions Blocks',
  },
  fields: [],
}
